#{
    %删除ref的时间.
    time_to_del_ref=> 100000,
    % 更新路由信息的时间
    time_to_update_router => 2000,
    % 每条路由生存周期.
    live_period_for_router_item => 10,
    % 要显示log的最低等级
    log_level => 10,
    config_from_file => true
}.
% 不可计算.
% config file in home dir.
